[package]
name = "media-websocket"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
media-core = { path = "../media-core" }

[dev-dependencies]
rtp = { path = "../rtp" }
