//! Cross-crate property-test harness for protocol and resource invariants.
//!
//! The executable properties live in `tests/properties.rs` so ordinary
//! workspace tests and hosted CI run them without a separate command.
