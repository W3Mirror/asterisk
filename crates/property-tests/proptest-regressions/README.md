# Property-test regressions

`proptest` persists minimized failing seeds beside the property source under
this directory. Commit any generated regression file with the fix that makes
the counterexample pass so ordinary workspace CI replays it permanently.
