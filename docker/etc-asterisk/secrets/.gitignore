# This whole directory holds real credential files (#include'd from
# ../pjsip.conf and ../ari.conf). Nothing here should ever be committed
# except the .example templates and this file itself.
*
!.gitignore
!*.example
