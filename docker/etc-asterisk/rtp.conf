; rtp.conf - minimal config for the Docker image.
;
; Keep this range in sync with:
;   - EXPOSE 10000-10100/udp in the Dockerfile
;   - the commented `ports:` block in compose.yml (only relevant if you
;     switch away from network_mode: host)

[general]
rtpstart=10000
rtpend=10100
